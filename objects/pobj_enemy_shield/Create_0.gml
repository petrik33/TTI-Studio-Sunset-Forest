sprite_idle = sprite_index;
surging = true;

image_alpha = 1;
animation_progress = 0;
//animation_speed = (pi) / (FRAME_RATE * 2);
animation_speed = (1) / (FRAME_RATE * 1.5);
target = noone;
duration = 0;

x_offset = 0;
y_offset = 0;